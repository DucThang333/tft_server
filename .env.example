# Copy to `.env` (gitignored). Used by `config/dev.exs` and Docker Compose.
#
# Neon: use the "URI" connection string from the Neon dashboard.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require&channel_binding=require
